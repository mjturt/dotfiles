#┃ ~/.config/feh/themes
#┣━━━━━━━━━━━━━━━━━━━━━
#┃ mjturt

# default
feh --scale-down --draw-actions --draw-filename

# others
imgidx --index --output-only .fehindex.jpg --limit-width 1024 \
       --thumb-width 128 --thumb-height 128 \

fs  --full-screen --hide-pointer --sort filename
